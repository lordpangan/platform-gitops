# Example XNetwork claim override — proves the override behavior.
apiVersion: platform.meridian.io/v1alpha1
kind: XNetwork
metadata:
  name: sandbox
  namespace: platform
spec:
  mode: prd
  cidr: 192.168.0.0/16
  tags:
    owner: platform-team
    env: sandbox
