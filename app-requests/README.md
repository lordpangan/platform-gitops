# app-requests/

The thin `WebApp` claims — one file per app, laid out as
`<namespace>/<kind>/<name>.yaml` so ArgoCD auto-discovers them.

Backstage's scaffolder writes files here. This folder is the developer-facing
surface (and the debugging escape hatch). Filled in during **Phase 5**.
